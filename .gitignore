node_modules/
.next/
.vercel/
.env*
!.env.example
*.tsbuildinfo
*.log

# Local visual QA output, not application source.
artifacts/

# Business outreach data is operational, not application source.
outreach/

# Local tool config, not application source — except the skills we
# deliberately vendor, which every session on this repo should get.
.claude/*
!.claude/skills/
